test:
    driver: mysql
    open: tpt:extreme@tcp(localhost:3306)/tpt_data?autocommit=true&parseTime=true

development:
    driver: mysql
    open: tpt:extreme@tcp(localhost:3306)/tpt_data?autocommit=true&parseTime=true

production:
    driver: mysql
    open: tpt:extreme@tcp(localhost:3306)/tpt_data?autocommit=true&parseTime=true
